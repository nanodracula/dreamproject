# Provider keys the edge runtime reads with Deno.env.get(). Self-hosted
# Supabase has no `supabase secrets set`: the values live in the Dokploy
# compose environment of the functions service, not in this repository and
# not on the deploying machine. See server/README.md.
OPENROUTER_API_KEY=
ELEVENLABS_API_KEY=
